apiVersion: v1
kind: Service
metadata:
  name: plantshield-frontend
  labels:
    app: plantshield-frontend
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: plantshield-frontend